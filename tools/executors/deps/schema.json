{
    "$schema": "http://json-schema.org/schema",
    "type": "object",
    "cli": "nx",
    "properties": {
      "project": {
        "type": "string",
        "description": "Name of the project containing where package.json should be updated"
      }
    }
  }